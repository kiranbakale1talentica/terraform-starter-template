# Service-First Infrastructure Blueprint

# Reusable modules should be instantiated from modules/ directory
# Example:
# module "service_app" {
#   source = "../../modules/ecs-service"
#   name   = var.project_name
#   env    = var.environment
# }
