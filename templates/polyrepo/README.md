# Polyrepo Template Boilerplate

Designed for component-isolated repositories with single-purpose state files.

Each polyrepo contains:
- `main.tf`, `variables.tf`, `outputs.tf`
- `providers.tf`, `backend.tf`
- Independent `.github/workflows/ci.yml` governance pipeline.
